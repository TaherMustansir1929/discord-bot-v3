version: '3.8'

services:
  discord-bot:
    build:
      context: ./discord-bot
      dockerfile: Dockerfile
    container_name: discord-bot
    env_file:
      - .env
    restart: unless-stopped
    # Extend this file by adding more services below
    # example-api:
    #   build:
    #     context: ./example-api
    #     dockerfile: Dockerfile
    #   container_name: example-api
    #   env_file:
    #     - .env
    #   ports:
    #     - "8000:8000"
    #   restart: unless-stopped
